{
  "skill_name": "hit-committer",
  "notes": "Each eval runs in an isolated git sandbox (local bare remote named 'hantop', no real GitHub). The subagent performs the full branch->atomic-commits->PR workflow; the PR is emitted as a gh command + body file instead of being created for real. Grading inspects the sandbox git history plus the emitted PR artifacts.",
  "evals": [
    {
      "id": 1,
      "name": "feature-plus-stray-fix",
      "scenario": "feature-plus-stray-fix",
      "prompt": "Commit everything and open a PR for me — I added the notifications feature (new module plus wired it into app.py) and also fixed that auth null-token check while I was in there.",
      "expected_output": "Work moved OFF protected main onto a feature/* branch; the unrelated auth fix split into its own commit ([fix]) separate from the feature commit(s) ([feat]); all messages in [type] Description format; PR targets base main with a test-record section; push uses the detected remote 'hantop'.",
      "files": [],
      "assertions": [
        {"text": "main has no new direct commits and a feature/* branch was created (protected-main guardrail honored)"},
        {"text": "Branch follows <category>/<desc> form with the feature/ category"},
        {"text": "The unrelated auth null-token fix is in its own commit, not lumped with the feature"},
        {"text": "Commit types are correct: feature is [feat], the auth fix is [fix]"},
        {"text": "Every commit title is [type] Description: lowercase type, capitalized English description, <=72 chars"},
        {"text": "The emitted PR targets base main"},
        {"text": "The PR body contains a test-record section (Discipline 5)"},
        {"text": "Push/PR use the detected remote 'hantop', not a hardcoded 'origin'"}
      ]
    },
    {
      "id": 2,
      "name": "bug-fix-with-issue",
      "scenario": "bug-fix-with-issue",
      "prompt": "I fixed the off-by-one in src/paginate.py — the last item on each page was getting dropped. Commit it and open a PR; this closes issue #17.",
      "expected_output": "A bug/* branch off main with a single [fix] commit whose title states intent (not a bare 'Update'); PR base main; PR body includes 'Closes #17' and a test-record section.",
      "files": [],
      "assertions": [
        {"text": "A bug/* branch was created off main; main has no new direct commits"},
        {"text": "Exactly one [fix] commit for the change, with an intent-focused title (not a bare 'Update'/'Change')"},
        {"text": "Commit title is [type] Description format, capitalized English, <=72 chars"},
        {"text": "The emitted PR targets base main"},
        {"text": "The PR body contains 'Closes #17'"},
        {"text": "The PR body contains a test-record section (Discipline 5)"}
      ]
    },
    {
      "id": 3,
      "name": "docs-only",
      "scenario": "docs-only",
      "prompt": "Just updated the README with a Configuration and License section. Can you commit that and open the PR?",
      "expected_output": "Docs-only change placed on a chore/* branch (the model has no docs/ branch) with a [docs] commit; main untouched; PR base main; test-record section present (N/A-with-reason acceptable for docs).",
      "files": [],
      "assertions": [
        {"text": "The change is placed on a chore/* branch (no dedicated docs/ branch exists in the model)"},
        {"text": "The commit type is [docs]"},
        {"text": "main has no new direct commits; the work moved onto a branch"},
        {"text": "The emitted PR targets base main"},
        {"text": "The PR body contains a test-record section (an N/A-with-reason entry is acceptable for docs-only)"}
      ]
    },
    {
      "id": 4,
      "name": "hotfix-on-release",
      "scenario": "hotfix-on-release",
      "prompt": "Urgent — the v1.1.0 release crashes on boot when device_profile is missing. I've patched src/app.py on the release line. Commit it as a hotfix and open the PR.",
      "expected_output": "Recognizes HEAD is the protected release/v1.1.0 and does NOT commit there; creates hotfix/v1.1.0-<desc> from it; [hotfix] commit; PR base is release/v1.1.0 (NOT main); test-record section present; reminds about Discipline 2 propagation to main + other in-use releases via port/*.",
      "files": [],
      "assertions": [
        {"text": "Did NOT commit on the protected release/v1.1.0; created a hotfix/v1.1.0-<desc> branch from it"},
        {"text": "The commit type is [hotfix]"},
        {"text": "The emitted PR targets base release/v1.1.0 (NOT main)"},
        {"text": "The PR body contains a test-record section (Discipline 5)"},
        {"text": "Reminds the user to propagate the hotfix to main + other in-use releases via port/* after merge/tag (Discipline 2)"}
      ]
    }
  ]
}
